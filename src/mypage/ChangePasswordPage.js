import React from 'react';
// You can build this component in the future.
// It would typically have fields for Old Password, New Password, and Confirm New Password.
const ChangePasswordPage = () => {
    return (
        <div style={{ padding: '20px 40px', backgroundColor: '#fff', borderRadius: '8px', boxShadow: '0 4px 8px rgba(0, 0, 0, 0.05)' }}>
            <h2 style={{ color: '#0094B2', marginBottom: '30px', paddingBottom: '15px', borderBottom: '2px solid #f0f2f5' }}>
                비밀번호 변경
            </h2>
            <p>비밀번호 변경 기능은 현재 준비 중입니다.</p>
        </div>
    );
};

export default ChangePasswordPage;
